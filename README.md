# SortedLinkedList

A simple sorted list implementation in PHP with type-safe nodes, comparator support, and unit tests.

## Usage

```bash
docker-compose up
```

Inside container
```bash
composer install
vendor/bin/phpunit
```
